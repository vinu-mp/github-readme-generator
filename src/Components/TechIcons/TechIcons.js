import IconWrapper from "../UI/IconWrapper/IconWrapper";
import techIcons from "../../Data/icons.json";

const TechIcons = () => {
  return <IconWrapper isIcon={true} data={techIcons} />;
};

export default TechIcons;
